# 问题解决记录


## 记录 1：Calculator 安全


问题：计算器如果使用 `eval` 会有代码执行风险。

Prompt：Review 一个最小 calculator tool，指出如何支持四则运算同时避免任意代码执行。

AI 输出摘要：使用 AST 白名单，只允许 `Constant`、`BinOp`、`UnaryOp` 和有限运算符。

最终决策：接受。支持 `+ - * / **`、括号和正负号；限制过大指数。

代码 / 测试证据：`miniagent/tools/calculator.py`、`test_calculator_rejects_dangerous_expression`。

## 记录 2：Session 隔离


问题：同一用户多个窗口容易发生上下文串线。

Prompt：检查 session 查询和 context 构建，哪些字段必须同时约束？

AI 输出摘要：所有 session 读取都应使用 `user_id + session_id`；context 只能读取当前 session messages 和 summary。

最终决策：接受。Todo 是用户级业务数据，但记录 source session。

代码 / 测试证据：`SQLiteRepository.get_session`、`ContextManager.build`、`test_session_context_is_isolated`。

## 记录 3：长对话压缩


问题：20 轮会话不能无限塞原文，但又要保留关键事实。

Prompt：给一个 MVP 可实现的 context compression，不引入向量库。

AI 输出摘要：结构化摘要 + 最近消息窗口；原文留库；摘要字段包含 facts、results、open_tasks、entities。

最终决策：接受。当前实现为启发式摘要，生产可替换为 LLM 摘要。

代码 / 测试证据：`miniagent/runtime/context.py`、`test_context_compression_keeps_fact_and_recent_window`。

## 记录 4：工具调用异常

问题：未知工具、参数错误、工具超时不能让 runtime 崩溃。

Prompt：设计 ToolExecutor 的错误包装与 trace 字段。

AI 输出摘要：白名单查找；参数校验；线程池 timeout；统一返回 `ToolResult(success=False)`；trace 记录成功/失败。

最终决策：接受。错误 observation 交还模型，模型可继续解释或修正。

代码 / 测试证据：`miniagent/runtime/tool_executor.py`、`test_unknown_tool_is_observed_not_crashed`。


## 记录 5：OpenAI-compatible 工具消息顺序

问题：调用工具后 DeepSeek 报错：`Messages with role 'tool' must be a response to a preceding message with 'tool_calls'`。

Prompt：检查 Agent Runtime 中 assistant tool_call、tool result 的消息保存和 context 重建顺序，找出为什么 OpenAI-compatible API 会拒绝请求。

AI 输出摘要：`tool` role 消息必须紧跟带 `tool_calls` 的 assistant 消息，并通过 `tool_call_id` 对应；历史 context 里不能出现孤立 tool result。

最终决策：接受。保存 assistant tool_call 消息，tool result 绑定 `tool_call_id`；ContextManager 增加 tool sequence sanitizer。

代码 / 测试证据：`miniagent/runtime/context.py`、`miniagent/runtime/agent.py`、`test_context_sanitizes_orphan_tool_result`、`test_context_sanitizes_incomplete_parallel_tool_call`。


## 记录 6：Todo 可点击操作

问题：Todo 最初只以文本或 Markdown 表格展示，用户不能直接点击完成或撤回。

Prompt：把 todo 工具结果设计成 chat 内的可点击 bot 消息，而不是全局侧边栏。

AI 输出摘要：解析结构化 todo tool result，渲染为当前会话内的 Todo 卡片；按钮调用 complete/reopen API；操作结果继续保存成会话消息。

最终决策：接受。Todo 不作为全局侧栏常驻展示，改为 session-scoped chat card；支持完成、撤回和已完成折叠。

代码 / 测试证据：`miniagent/web/index.html`、`miniagent/server.py`、`SQLiteRepository.complete_todo`、`SQLiteRepository.reopen_todo`。


## 记录 7：相对时间归一化

问题：用户说“明天五点到上海的高铁”，Todo 中仍显示“明天”，没有落成具体日期和时间。

Prompt：Agent Runtime 如何处理“今天/明天/后天/下午2点/五点”这类相对时间，哪些由模型做，哪些由工具兜底？

AI 输出摘要：Runtime 注入当前本地时间；LLM 负责理解语义；Todo 工具做最终归一化；不能只依赖模型计算当前日期。

最终决策：接受。Context 注入当前时间；TodoTool 支持相对日期和中文数字时间；写入 `due_at`，前端展示绝对时间。

代码 / 测试证据：`miniagent/runtime/context.py`、`miniagent/tools/todo.py`、`test_todo_tool_normalizes_relative_due_dates`。
