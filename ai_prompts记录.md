# MiniAgent Runtime：AI Prompt 记录

---

## 01

### Prompt

```text
我准备开发一个轻量级的 MiniAgent Runtime，用于理解和实现 Agent 的核心运行机制。

项目计划使用 Python 标准库为主实现 Runtime 和 HTTP 服务，使用 SQLite 做持久化，
测试层使用 unittest，并保持兼容 pytest 收集；模型层接入 OpenAI-compatible LLM API。

首个版本计划包含以下能力：

- 接收用户输入并调用 LLM；
- 由 LLM 判断直接回答或调用工具；
- 支持 calculator、search、weather 和 todo 工具；
- 通过名称、描述和参数 Schema 注册工具；
- 将工具结果返回给模型并继续执行；
- 支持多个相互独立的 Session；
- 保存历史消息并支持多轮追问；
- Context 过长时进行基础压缩；
- 设置最大执行轮次和异常处理；
- 记录模型调用和工具执行 Trace；
- 使用 FakeLLM 和真实 LLM 分别完成离线自动化测试与冒烟测试。

请作为技术方案 Review 助手，帮助我完成以下工作：

1. 检查当前功能范围是否适合作为最小可用版本；
2. 划分 Agent Runtime、LLM、Tool、Session、Context 和 Trace 的模块职责；
3. 梳理一次用户请求在 Runtime 内部的完整执行流程；
4. 补充容易遗漏的异常场景和边界条件；
5. 设计合理的测试分层；
6. 指出可能存在的过度设计；
7. 将确认后的内容整理为一份项目 PRD 初稿。

对于存在多种实现方式的部分，请比较优缺点并标记待决策项，
不要直接扩大项目范围。
```



---

## 02：

```text
当前 MiniAgent Runtime 项目已经按照既定 PRD 完成主要开发。

请直接检查当前仓库中的项目结构、核心代码和测试，从以下方面进行 Review：

1. Agent Loop 是否由项目自行控制，是否存在对现成 Agent 框架的隐式依赖；
2. 用户消息、assistant 工具调用和 tool result 的消息顺序是否正确；
3. tool result 是否通过 tool_call_id 与对应工具调用正确关联；
4. 单工具、多工具和连续工具调用是否能够正常执行；
5. 工具参数是否在执行前经过 Schema 校验；
6. 是否存在无限循环、重复工具调用或错误终止问题；
7. 最大执行轮次和终止原因是否清晰；
8. 不同 Session 是否可能发生上下文串话或数据越权；
9. Context 构建和压缩是否可能丢失关键消息，
   或拆开工具调用与工具结果；
10. 异常发生后，Session 状态和消息记录是否仍然一致；
11. Trace 是否覆盖模型请求、工具调用、异常和终止过程；
12. 是否存在明显的安全风险、重复逻辑或不必要的模块耦合。

请优先检查以下模块：

- Agent Runtime 主循环；
- LLM Adapter；
- Tool Registry 和 ToolExecutor；
- Session 与 Message 持久化；
- ContextManager；
- Trace；
- 相关测试。

请按照以下格式输出：

- 问题位置；
- 问题描述；
- 风险等级；
- 触发场景；
- 最小修改建议；
- 建议增加的测试。

不要重写整个项目，也不要增加当前 PRD 范围以外的复杂功能。
```

---

## 03：

```text
当前 MiniAgent Runtime 项目已经完成开发，准备进行最终整理。

请直接检查当前仓库中的：

- PRD；
- README；
- 项目目录；
- 核心实现；
- 测试用例；
- 测试配置；
- Trace 示例；
- 环境变量示例。

请重点检查：

1. PRD 中声明的功能是否都有对应实现；
2. README 是否描述了实际未实现的能力；
3. 已实现的关键功能是否缺少文档说明；
4. 是否覆盖普通回答、单工具调用、多工具调用和工具追问；
5. 是否覆盖 Session 隔离、历史恢复和 Context 压缩；
6. 是否覆盖非法参数、未知工具、工具异常、超时、
   重复调用和最大轮次；
7. FakeLLM 自动化测试和真实 LLM Smoke Test 的职责是否清楚；
8. 运行命令、环境变量和数据库初始化步骤是否完整；
9. Trace 是否能够展示一次完整的 Agent 执行过程；
10. PRD、README、代码和测试之间是否存在矛盾；
11. 是否有临时文件、调试代码、硬编码密钥或无用依赖需要清理；
12. 哪些功能适合录屏重点展示；
13. 哪些限制应该明确写入 README。

请先输出：

1. 提交前必须修复的问题；
2. 建议修复的问题；
3. 可以作为已知限制保留的问题；
4. 缺失的测试和文档内容。

不要虚构测试结果，不要修改与当前项目无关的代码。
```

